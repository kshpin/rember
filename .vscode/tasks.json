{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cargo",
            "command": "build",
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/backend"
            },
            "label": "rember: build backend"
        },
        {
            "type": "cargo",
            "command": "build",
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/clients/tui"
            },
            "label": "rember: build tui"
        }
    ]
}
